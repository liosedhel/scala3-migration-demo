package org.virtuslab.migration.demo.ui;

import org.virtuslab.migration.demo.utils.*;

class Reproduce {

    public Reproduce() {
        //dateMethod(new GenerateTimeRange().timeRange().from().get());
    }

    public void dateMethod(java.time.LocalDate localDate) {

    }
}
