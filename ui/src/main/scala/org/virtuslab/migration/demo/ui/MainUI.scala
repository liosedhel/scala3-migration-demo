package org.virtuslab.migration.demo.ui

import org.virtuslab.migration.demo.utils.Utils

object MainUI {
  def main(args: Array[String]): Unit = {
    println("Hello world UI")
    Utils.printHelloWorld()
  }
}
